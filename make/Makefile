# Alternative GNU Make workspace makefile autogenerated by Premake

ifndef config
  config=prod
endif

ifndef verbose
  SILENT = @
endif

ifeq ($(config),prod)
  EchoSrv_config = prod

else ifeq ($(config),verbose)
  EchoSrv_config = verbose

else ifeq ($(config),vsmall)
  EchoSrv_config = vsmall

else ifeq ($(config),gpt)
  EchoSrv_config = gpt

else
  $(error "invalid configuration $(config)")
endif

PROJECTS := EchoSrv

.PHONY: all clean help $(PROJECTS) 

all: $(PROJECTS)

EchoSrv:
ifneq (,$(EchoSrv_config))
	@echo "==== Building EchoSrv ($(EchoSrv_config)) ===="
	@${MAKE} --no-print-directory -C . -f EchoSrv.make config=$(EchoSrv_config)
endif

clean:
	@${MAKE} --no-print-directory -C . -f EchoSrv.make clean

help:
	@echo "Usage: make [config=name] [target]"
	@echo ""
	@echo "CONFIGURATIONS:"
	@echo "  prod"
	@echo "  verbose"
	@echo "  vsmall"
	@echo "  gpt"
	@echo ""
	@echo "TARGETS:"
	@echo "   all (default)"
	@echo "   clean"
	@echo "   EchoSrv"
	@echo ""
	@echo "For more information, see https://github.com/premake/premake-core/wiki"